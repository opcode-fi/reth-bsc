[package]
name = "rex-proto"
version = "0.1.0"
edition = "2021"
description = "Protocol buffer definitions for the reth-rex remote ExEx gRPC service"

[dependencies]
tonic = "0.11"
prost = "0.12"

[build-dependencies]
tonic-build = "0.11"
